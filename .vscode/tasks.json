{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "echo Hello"
        },
        {
            "label": "MkDocs Development Server",
            "type": "docker-run",
            "dockerRun": {
                "image": "squidfunk/mkdocs-material",
                "ports": [
                    { "hostPort": 8000, "containerPort": 8000 }
                ],
                "volumes": [
                    { "localPath": "${workspaceFolder}", "containerPath": "/docs" }
                ],
                "remove": true
            }
        }
    ]
}